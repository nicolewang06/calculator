const form = document.getElementById('sign-up');

form.addEventListener('submit', (e) => {
    e.preventDefault(); // preventing resfresh when submitting form

    const username = document.getElementById('username-input'); // input field
    const password = document.getElementById('password-input'); // input field
    If

    console.log(username.value, password.value); // inputField.value = text inside of input field
})
// const hi = document.getElementById('hi');
// hi